import React from 'react';
import { Github } from 'lucide-react';

interface Project {
  title: string;
  description: string;
  tech: string[];
  githubUrl: string;
  demoUrl?: string;
}
const Projects = () => {
    return (
        <div>

        </div>
    )
};

export default Projects;